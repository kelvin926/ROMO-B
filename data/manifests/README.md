# Artifact manifests

Copy `example.yaml` for each real bag, PCD, or map bundle. `sha256` and
`size_bytes` must match the source file. Replace `uri: pending` before the first
real-data acceptance test so other lab members can retrieve the same artifact.
